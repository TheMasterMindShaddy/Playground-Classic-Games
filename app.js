(()=>{const views=[...document.querySelectorAll('.view')];function go(id){views.forEach(v=>v.classList.toggle('active',v.id===id));location.hash=id;window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-go]').forEach(e=>e.addEventListener('click',()=>go(e.dataset.go)));document.querySelectorAll('.filters button').forEach(b=>b.addEventListener('click',()=>{document.querySelector('.filters .selected').classList.remove('selected');b.classList.add('selected');document.querySelectorAll('.game-tile').forEach(t=>t.style.display=b.dataset.filter==='all'||t.dataset.kind===b.dataset.filter?'block':'none')}));let id=location.hash.slice(1);if(id&&document.getElementById(id))go(id)})();
